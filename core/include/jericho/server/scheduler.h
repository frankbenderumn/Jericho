#ifndef SCHEDULER_H_
#define SCHEDULER_H_

/* Ignore for now

#include <pthread.h>

#include "server/defs.h"

typedef int (*__SchedulerStrategy)(Any args);

typedef struct {
    __SchedulerStrategy strategy;
} Scheduler;

*/

#endif
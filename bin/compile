#!/bin/bash
./bin/kill-cluster
# ./ext/celerity/bin/compile
# mkdir cmake-build
cd cmake-build
cmake --build .. --target clean
# make clean
make -j`nproc`
# cd ..

# ./cmake-build/server 8080 5 ./nebulaui/frontend